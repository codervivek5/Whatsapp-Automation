from django.db import models
import uuid
# Create your models here.
# class AutoMassage(models.Model):
#     id = models.UUIDField(primary_key=True, uuid = uuid.uuid4, editable=False )
#     mobile_no = models.CharField(max_length=10)
